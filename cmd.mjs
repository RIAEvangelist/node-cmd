import commandline from './cmd.js';

const {
    run,
    runSync,
    runPromise,
    runPromisified,
    runFile,
    runFileSync,
    runFilePromise,
    runFilePromisified,
    runStream
} = commandline;

export {
    run,
    runSync,
    runPromise,
    runPromisified,
    runFile,
    runFileSync,
    runFilePromise,
    runFilePromisified,
    runStream
};

export default commandline;
